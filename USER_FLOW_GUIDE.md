# 🔄 User Flow Guide - Affiliance Platform

## نظرة عامة على رحلة كل مستخدم

---

## 👔 Company (الشركة) - User Journey

### **المرحلة 1: التسجيل والموافقة**

#### 1.1 التسجيل
```
POST /api/account/campany_Register
```
**البيانات المطلوبة:**
- معلومات الشركة (الاسم، العنوان، الهاتف، الموقع)
- ملفات مطلوبة:
  - Commercial Register (السجل التجاري)
  - Logo (شعار الشركة)
  - Tax ID (الرقم الضريبي)
- معلومات المستخدم (Email, Password)

**الحالة بعد التسجيل:**
- ✋ **Pending** - في انتظار موافقة الـ Admin
- ❌ لا يمكن تسجيل الدخول حتى الموافقة

---

#### 1.2 انتظار موافقة الـ Admin
**Admin Actions:**
- يراجع بيانات الشركة والمستندات
- يوافق (`Approve`) أو يرفض (`Reject`)

**بعد الموافقة:**
- ✅ الحالة تتغير إلى **Active**
- ✅ يمكن تسجيل الدخول

---

### **المرحلة 2: تسجيل الدخول**

```
POST /api/account/login
{
  "email": "company@example.com",
  "password": "Company@123"
}
```

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "token": "JWT_TOKEN",
    "refreshToken": "REFRESH_TOKEN",
    "companyId": 1,
    "role": "Company"
  }
}
```

---

### **المرحلة 3: إنشاء حملة (Campaign)**

```
POST /api/campaign
Authorization: Bearer {TOKEN}
```

**البيانات:**
```json
{
  "title": "Summer Sale Campaign 2026",
  "description": "Promote our summer products",
  "categoryId": 1,
  "commissionType": 0,  // 0=Percentage, 1=Fixed
  "commissionValue": 15.0,
  "budget": 10000.00,
  "startDate": "2026-03-01T00:00:00",
  "endDate": "2026-06-30T23:59:59",
  "promotionalMaterials": "https://example.com/materials.zip",
  "trackingBaseUrl": "https://example.com/track"
}
```

**الحالة بعد الإنشاء:**
- 📋 **Pending** - تحتاج موافقة Admin
- ❌ غير مرئية للـ Marketers حتى الموافقة

---

### **المرحلة 4: موافقة Admin على الحملة**

**Admin Endpoint:**
```
PUT /api/campaign/{id}/admin/approve
```

**بعد الموافقة:**
- ✅ الحملة تصبح **Active**
- 👁️ مرئية للـ Marketers في القوائم العامة
- 🎯 تظهر في الـ Recommended Campaigns

---

### **المرحلة 5: إدارة طلبات الـ Marketers**

#### 5.1 عرض الطلبات
```
GET /api/campaign/{campaignId}/applications
```

**تصنيف الطلبات:**
- Pending (في الانتظار)
- Accepted (تمت الموافقة)
- Rejected (مرفوض)
- Withdrawn (انسحب المسوق)

---

#### 5.2 الموافقة على طلب
```
POST /api/campaign/applications/{applicationId}/approve
{
  "note": "Welcome! Here are your promotional guidelines..."
}
```

**ماذا يحدث تلقائياً؟**
1. ✅ الطلب يتحول إلى **Accepted**
2. 🔗 **يتم إنشاء Tracking Link تلقائياً** للمسوق
3. 📧 إشعار للمسوق بالموافقة
4. 📊 المسوق يبدأ التتبع والعمولات

**Tracking Link Example:**
```
https://example.com/track?cid=1&mid=5&code=ABC123XYZ
```

---

#### 5.3 رفض طلب
```
POST /api/campaign/applications/{applicationId}/reject
{
  "note": "Sorry, your profile doesn't match our target audience"
}
```

---

### **المرحلة 6: مراقبة الحملة**

#### 6.1 إحصائيات الحملة
```
GET /api/campaign/{id}/statistics?from=2026-03-01&to=2026-03-31
```

**البيانات المتاحة:**
- عدد الـ Clicks
- عدد الـ Conversions (المبيعات)
- Total Revenue
- Total Commission Paid
- Conversion Rate
- ROI (Return on Investment)
- عدد المسوقين النشطين

---

#### 6.2 عرض حملاتي الخاصة
```
GET /api/campaign/my-campaigns
```

---

### **المرحلة 7: إدارة دورة حياة الحملة**

#### 7.1 إيقاف الحملة مؤقتاً
```
PUT /api/campaign/{id}/pause
```
- يوقف قبول طلبات جديدة
- الروابط الحالية تستمر في العمل

---

#### 7.2 استئناف الحملة
```
PUT /api/campaign/{id}/resume
```

---

#### 7.3 تحديث الحملة
```
PUT /api/campaign/{id}
{
  "description": "Updated description",
  "budget": 15000.00,
  "commissionValue": 20.0
}
```

---

#### 7.4 إنهاء/حذف الحملة
```
DELETE /api/campaign/{id}
```

**سلوك الحذف:**
- إذا كان فيه نشاط (applications, clicks): **Soft Delete** (إخفاء فقط)
- إذا لم يكن فيه نشاط: **Hard Delete** (حذف نهائي)

---

### **المرحلة 8: مراجعة الأداء**

```
GET /api/campaign/company/{companyId}
```
- عرض كل الحملات
- التصفية حسب الحالة
- البحث بالكلمات المفتاحية

---

## 🎯 Marketer (المسوق) - User Journey

### **المرحلة 1: التسجيل**

```
POST /api/account/register-marketer
```

**البيانات المطلوبة:**
- معلومات شخصية (الاسم، Email, Password)
- معلومات تسويقية:
  - Bio (نبذة عنك)
  - Niche (تخصصك)
  - Social Media Links
  - Audience Size
- ملفات:
  - CV
  - National ID
  - Portfolio (اختياري)

**الحالة بعد التسجيل:**
- ✅ **Active** مباشرة (لا يحتاج موافقة Admin)
- ✅ يمكن تسجيل الدخول فوراً

---

### **المرحلة 2: تسجيل الدخول**

```
POST /api/account/login
{
  "email": "marketer@example.com",
  "password": "Marketer@123"
}
```

---

### **المرحلة 3: استكشاف الحملات**

#### 3.1 عرض الحملات النشطة
```
GET /api/campaign/active
```

---

#### 3.2 البحث عن حملات
```
GET /api/campaign/search?keyword=fashion&minCommission=10
```

**معايير البحث:**
- الكلمات المفتاحية
- الفئة (Category)
- نطاق العمولة
- نوع العمولة (نسبة/ثابت)
- الحالة (Active)

---

#### 3.3 الحملات الموصى بها (AI-Powered)
```
GET /api/campaign/recommended?limit=10
Authorization: Bearer {TOKEN}
```

**كيف يعمل الذكاء الاصطناعي؟**
- يحلل ملفك الشخصي (Niche, Skills)
- يحلل سجل طلباتك السابقة
- يقترح حملات مناسبة لتخصصك
- يرتب حسب احتمالية النجاح

---

### **المرحلة 4: التقديم على حملة**

```
POST /api/campaign/{id}/apply
Authorization: Bearer {TOKEN}
```

**ماذا يحدث؟**
1. 📨 يتم إنشاء **Application** بحالة **Pending**
2. 🔔 الشركة تتلقى إشعار
3. ⏳ انتظار رد الشركة

**الحالات الممكنة:**
- ✅ **Accepted** - تمت الموافقة
- ❌ **Rejected** - تم الرفض
- 🔙 **Withdrawn** - انسحبت أنت

---

### **المرحلة 5: الموافقة والحصول على Tracking Link**

**عند موافقة الشركة:**

```
GET /api/trackinglink/my-links
```

**الاستجابة:**
```json
{
  "data": [{
    "id": 1,
    "campaignId": 5,
    "campaignTitle": "Summer Sale",
    "trackingCode": "ABC123XYZ",
    "trackingUrl": "https://example.com/track?cid=5&mid=3&code=ABC123XYZ",
    "clicks": 150,
    "conversions": 12,
    "totalRevenue": 5000.00,
    "commission": 750.00,
    "status": "Active"
  }]
}
```

---

### **المرحلة 6: الترويج والتسويق**

#### 6.1 استخدام الرابط الخاص
- انشر الرابط على:
  - Social Media
  - Blog
  - YouTube
  - Email Marketing
  - أي قناة تسويقية

---

#### 6.2 تتبع الأداء
```
GET /api/trackinglink/{id}/statistics
```

**المقاييس المتاحة:**
- عدد النقرات (Clicks)
- المبيعات (Conversions)
- معدل التحويل (Conversion Rate)
- الإيرادات الإجمالية
- عمولتك المكتسبة

---

### **المرحلة 7: كسب العمولات**

**أنواع العمولات:**

1. **Commission Type: Percentage**
   - مثال: 15% من كل عملية بيع
   - إذا بيع منتج بـ $100 → تحصل على $15

2. **Commission Type: Fixed**
   - مثال: $50 لكل عملية بيع
   - بغض النظر عن قيمة المشتريات

**تتبع الرصيد:**
```
GET /api/marketer/balance
```

---

### **المرحلة 8: كتابة مراجعة للحملة**

```
POST /api/review
{
  "campaignId": 5,
  "rating": 5,
  "comment": "Excellent campaign! High conversion rate and fast commission payments",
  "isRecommended": true
}
```

**الشروط:**
- يجب أن تكون مشترك في الحملة
- يجب أن يكون لديك نشاط (clicks/conversions)

---

### **المرحلة 9: الانسحاب من حملة**

```
POST /api/campaign/applications/{applicationId}/withdraw
```

**متى يمكن الانسحاب؟**
- قبل الموافقة: ✅ في أي وقت
- بعد الموافقة: ⚠️ حسب شروط الحملة

---

## 👨‍💼 Admin (المدير) - User Journey

### **المرحلة 1: تسجيل الدخول**

```
POST /api/account/login
{
  "email": "admin@affiliance.com",
  "password": "Admin@123"
}
```

*(حساب الـ Admin يتم إنشاؤه تلقائياً عبر Database Seeding)*

---

### **المرحلة 2: إدارة الشركات**

#### 2.1 عرض طلبات التسجيل
```
GET /api/company/pending
```

---

#### 2.2 الموافقة على شركة
```
POST /api/company/{id}/approve
```

**ماذا يحدث؟**
- الشركة تصبح **Active**
- يمكنها تسجيل الدخول
- تستطيع إنشاء حملات

---

#### 2.3 رفض شركة
```
POST /api/company/{id}/reject
{
  "reason": "Incomplete documents"
}
```

---

### **المرحلة 3: إدارة الحملات**

#### 3.1 عرض الحملات المعلقة
```
GET /api/campaign?status=Pending
```

---

#### 3.2 الموافقة على حملة
```
PUT /api/campaign/{id}/admin/approve
{
  "responseNote": "Approved - meets all guidelines"
}
```

**النتيجة:**
- الحملة تصبح **Active**
- تظهر للـ Marketers
- يمكن التقديم عليها

---

#### 3.3 رفض حملة
```
PUT /api/campaign/{id}/admin/reject
{
  "responseNote": "Violates advertising policies - misleading claims"
}
```

---

### **المرحلة 4: المراقبة العامة**

```
GET /api/campaign
GET /api/marketer
GET /api/company
GET /api/review
```

- مراقبة كل الأنشطة
- مراجعة المراجعات
- حل النزاعات
- إدارة المحتوى

---

## 📊 Complete User Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        AFFILIANCE PLATFORM                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   COMPANY    │         │   MARKETER   │         │    ADMIN     │
└──────┬───────┘         └──────┬───────┘         └──────┬───────┘
       │                        │                        │
       │ 1. Register            │ 1. Register            │ Auto-Created
       │    (With Docs)         │    (Profile)           │
       ├─────────┐              └────────┐               │
       │ Status: │                       │               │
       │ Pending │              Status: Active           │
       │         │                       │               │
       │         ▼                       ▼               │
       │    ⏳ Wait Admin         ✅ Login Ready         │
       │         │                       │               │
       │         │ 2. Admin Reviews      │               │
       │         └──────────────────────►│◄──────────────┘
       │                                 │
       │         ◄───────────────────────┤
       │         Approve/Reject          │
       │                                 │
       ▼                                 │
   ✅ Login                              │
       │                                 │
       │ 3. Create Campaign              │
       ├────────────┐                    │
       │  Status:   │                    │
       │  Pending   │                    │
       │            │                    │
       │            │ 4. Admin Reviews   │
       │            └───────────────────►│
       │                                 │
       │            ◄────────────────────┤
       │            Approve              │
       │                                 │
       │  Campaign Active                │
       │      │                          │
       │      └─────────────────────────►│
       │         Visible to All          │
       │                                 │
       │                         5. Browse Campaigns
       │                                 │
       │                         6. Apply
       │                                 │
       │◄────────────────────────────────┤
       │        Application              │
       │                                 │
       │ 7. Review Application           │
       │    Approve/Reject               │
       ├────────────────────────────────►│
       │                                 │
       │    ✅ Auto Create               │
       │       Tracking Link             │
       ├────────────────────────────────►│
       │                                 │
       │                         8. Start Promoting
       │                                 │
       │                         9. Generate Clicks
       │◄────────────────────────────────┤
       │        Track Conversions        │
       │                                 │
       │ 10. Calculate Commission        │
       ├────────────────────────────────►│
       │                                 │
       │                         💰 Earn Money
       │                                 │
       │ 11. View Statistics             │
       │◄───────────────────────────────►│
       │                                 │
       │                         12. Submit Review
       │◄────────────────────────────────┤
       │                                 │
       ▼                                 ▼
```

---

## 🎯 Key Business Rules

### **Commission Calculation**

**Percentage-based:**
```
Commission = (Product Price × Commission Rate) / 100
Example: $100 × 15% = $15
```

**Fixed-based:**
```
Commission = Fixed Amount per Conversion
Example: $50 per sale (regardless of price)
```

---

### **Tracking Link Format**
```
https://example.com/track?cid={campaignId}&mid={marketerId}&code={uniqueCode}
```

**Parameters:**
- `cid`: Campaign ID
- `mid`: Marketer ID  
- `code`: Unique tracking code (للأمان ومنع التلاعب)

---

### **Status Flow**

**Company:**
```
Pending → Active/Rejected
```

**Campaign:**
```
Pending → Active/Rejected → Paused → Active → Completed/Deleted
```

**Application:**
```
Pending → Accepted/Rejected/Withdrawn
```

**Tracking Link:**
```
Active → Paused → Active → Expired
```

---

## 📱 Integration Points

### **للشركات:**
- Webhook عند كل conversion
- API لتحديث inventory
- Dashboard للإحصائيات الحية

### **للمسوقين:**
- API للحصول على بيانات الأداء
- Webhook عند الموافقات
- Mobile App للتتبع

### **للـ Admin:**
- Dashboard شامل
- Analytics و Reports
- Fraud Detection alerts

---

## 🔐 Security Measures

1. **JWT Authentication** - كل الـ endpoints محمية
2. **Role-based Authorization** - كل دور له صلاحيات محددة
3. **Unique Tracking Codes** - لمنع التلاعب بالروابط
4. **Rate Limiting** - لمنع الـ abuse
5. **Input Validation** - على كل البيانات المدخلة
6. **File Upload Security** - فحص الملفات المرفوعة

---

## 📈 Success Metrics

### **للشركات:**
- ROI (Return on Investment)
- Cost per Acquisition (CPA)
- Active Marketers Count
- Campaign Conversion Rate

### **للمسوقين:**
- Total Earnings
- Click-through Rate (CTR)
- Conversion Rate
- Average Commission per Sale

### **للـ Platform:**
- Total Campaigns Active
- Total Revenue Generated
- User Satisfaction (Reviews)
- Platform Commission Share

---

## 🎓 Best Practices

### **للشركات:**
1. ✅ اختر commission rate جذاب للمسوقين
2. ✅ وفر promotional materials عالية الجودة
3. ✅ رد على الطلبات بسرعة
4. ✅ راقب الأداء وعدّل الاستراتيجية

### **للمسوقين:**
1. ✅ اختر حملات تناسب جمهورك
2. ✅ استخدم multiple channels للترويج
3. ✅ تابع الإحصائيات باستمرار
4. ✅ اكتب مراجعات مفيدة

### **Platform Tips:**
1. 🎯 استخدم الـ AI Recommendations
2. 📊 راقب الـ Analytics
3. 🔔 فعّل الإشعارات
4. 💬 تواصل مع الدعم عند الحاجة

---

**تم إعداد هذا الدليل بناءً على Affiliance API v1.0**  
*Last Updated: February 9, 2026*
